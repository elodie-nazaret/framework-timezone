Order deny,allow
Allow from all

<FilesMatch "^.*(?<!\.php)$">
	Allow from all
</FilesMatch>

<Files routing.php >
	Allow from all
</Files>

Options +FollowSymlinks
RewriteEngine on
RewriteRule (.+(?<!routing))\.php$ /timezone/routing.php?url=$1 [QSA] [L]